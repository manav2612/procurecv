services:
  db:
    image: postgres:16-alpine
    restart: unless-stopped
    environment:
      POSTGRES_USER: speech_to_text
      POSTGRES_PASSWORD: speech_to_text
      POSTGRES_DB: speech_to_text
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U speech_to_text"]
      interval: 5s
      timeout: 5s
      retries: 10

  # Full single-service build (frontend baked in — see Dockerfile). Not
  # needed for day-to-day dev (run `uvicorn --reload` + `npm run dev`
  # instead, see README.md) — this is for testing the actual deploy artifact
  # locally: `docker compose up --build app`.
  app:
    build:
      context: .
      dockerfile: Dockerfile
    restart: unless-stopped
    environment:
      DATABASE_URL: postgresql+psycopg2://speech_to_text:speech_to_text@db:5432/speech_to_text
      WHISPER_MODEL_SIZE: ${WHISPER_MODEL_SIZE:-medium}
    ports:
      - "8000:8000"
    depends_on:
      db:
        condition: service_healthy
    volumes:
      # Persists the downloaded Whisper model across container restarts —
      # without this, every `docker compose up` re-downloads it.
      - whisper_cache:/root/.cache/huggingface

volumes:
  pgdata:
  whisper_cache:
